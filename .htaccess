# =============================================
# APACHE CONFIGURATION FOR BASKAR'S PORTFOLIO
# =============================================

# Enable mod_rewrite module
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Redirect non-www to www (optional)
    # RewriteCond %{HTTP_HOST} ^example\.com$ [NC]
    # RewriteRule ^(.*)$ https://www.example.com/$1 [R=301,L]
    
    # Force HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>

# =============================================
# SECURITY HEADERS
# =============================================

<IfModule mod_headers.c>
    # Prevent framing/clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Prevent mime-type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Enable XSS protection
    Header always set X-XSS-Protection "1; mode=block"
    
    # Referrer policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Content Security Policy
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; font-src 'self' https://cdnjs.cloudflare.com; img-src 'self' data: https:; connect-src 'self' https://formspree.io https://api.emailjs.com"
    
    # Permissions policy
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# =============================================
# COMPRESSION & CACHING
# =============================================

# Enable Gzip Compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
    AddEncoding gzip .js .css .html
</IfModule>

# Browser caching
<IfModule mod_expires.c>
    ExpiresActive On
    
    # HTML files - cache for 30 minutes
    ExpiresByType text/html "access plus 30 minutes"
    
    # CSS and JavaScript - cache for 1 year
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    
    # Images - cache for 1 year
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    
    # Fonts - cache for 1 year
    ExpiresByType font/ttf "access plus 1 year"
    ExpiresByType font/otf "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    
    # Default - 5 days
    ExpiresDefault "access plus 5 days"
</IfModule>

# Set cache control headers
<IfModule mod_headers.c>
    # Cache static assets
    <FilesMatch "\.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|otf)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # Don't cache HTML
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "public, max-age=1800, must-revalidate"
    </FilesMatch>
</IfModule>

# =============================================
# INDEX FILES
# =============================================

DirectoryIndex index.html index.htm

# =============================================
# FILE PERMISSIONS
# =============================================

# Deny access to hidden files and directories
<FilesMatch "^\.|^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Deny access to specific file types
<FilesMatch "\.(env|ini|log|config)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# =============================================
# MIME TYPES
# =============================================

<IfModule mod_mime.c>
    AddType font/ttf .ttf
    AddType font/otf .otf
    AddType font/woff .woff
    AddType font/woff2 .woff2
    AddType image/svg+xml .svg
    AddEncoding gzip .svgz
    AddType text/javascript .js
    AddType application/json .json
</IfModule>

# =============================================
# CUSTOM ERROR PAGES
# =============================================

ErrorDocument 404 /404.html
ErrorDocument 500 /500.html

# =============================================
# DIRECTORY PROTECTION
# =============================================

# Prevent directory listing
<IfModule mod_autoindex.c>
    Options -Indexes
</IfModule>

# =============================================
# FILE ACCESS
# =============================================

# Disable script execution in upload directory (if you add one)
<Directory "/path/to/uploads">
    php_flag engine off
    AddType text/plain .php .php3 .php4 .php5 .php6 .php7 .phps
</Directory>

# =============================================
# PERFORMANCE OPTIMIZATION
# =============================================

# Reduce DNS lookups
<IfModule mod_headers.c>
    Header set X-DNS-Prefetch-Control "on"
</IfModule>

# Keep alive connections
<IfModule mod_headers.c>
    Header set Connection "Keep-Alive"
</IfModule>

# =============================================
# CONDITIONAL COMMENTS FOR OLDER BROWSERS
# =============================================

# Prevent access to older JPEG files when WebP is supported
# <IfModule mod_negotiation.c>
#     Options +MultiViews
#     AddType image/webp .webp
# </IfModule>
